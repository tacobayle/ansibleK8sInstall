---
- hosts: master, workers
  gather_facts: yes
  become_user: root
  become_method: sudo

  tasks:

    - debug:
        msg: "{{ kubernetes }}"

    - name: patching /etc/systemd/system/kubelet.service.d/10-kubeadm.conf
      shell: |
        ip=$(ip -f inet addr show {{ kubernetes.ifApi }} | awk '/inet / {print $2}' | awk -F/ '{print $1}')
        sed '${s/$/ --node-ip '$ip'/}' /etc/systemd/system/kubelet.service.d/10-kubeadm.conf
      ignore_errors: no
      become: true


#  ip=$(ip -f inet addr show ens224 | awk '/inet / {print $2}' | awk -F/ '{print $1}') ; sudo sed '${s/$/ --node-ip '$ip'/}' /etc/systemd/system/kubelet.service.d/10-kubeadm.conf
#  sudo systemctl daemon-reload
#  sudo systemctl restart kubelet